## 🗂️ NumPy Learning Plan

### **Module 1: Introduction & Arrays**
- Why NumPy exists (speed vs Python lists).
- Creating arrays (`np.array`, `np.zeros`, `np.ones`, `np.arange`, `np.linspace`).
- Array attributes: shape, dtype, ndim.
- Under the hood: contiguous memory blocks, C-optimizations.

### **Module 2: Indexing, Slicing, Reshaping**
- Accessing elements, slicing ranges.
- Boolean indexing & fancy indexing.
- Reshaping arrays (`reshape`, `ravel`, `flatten`).
- Broadcasting rules (how NumPy handles different shapes).

### **Module 3: Mathematical Operations**
- Element-wise operations.
- Aggregate functions (`sum`, `mean`, `std`, `max`, `min`).
- Linear algebra basics (`dot`, `matmul`, `transpose`).
- Real-world use: feature scaling, vectorized computations.

### **Module 4: Practical Applications**
- Random numbers (`np.random`).
- Data preprocessing (normalization, shuffling).
- Image manipulation with arrays.
- Performance comparison: Python loops vs NumPy vectorization.

### **Module 5: Revision & Mini Project**
- Build a **Linear Algebra Toolkit** (matrix multiplication, determinant, inverse).
- Practice problems.
- Assignment: Implement a mini dataset preprocessing pipeline using NumPy.